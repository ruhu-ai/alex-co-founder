# Discover funding opportunities

Goal: return a bounded candidate set for the founder's stated funding need.

Use only the supplied current-workspace context and the exact registered
`opportunity.search@1.0.0` capability. Preserve source provenance, uncertainty,
eligibility unknowns, and conflicts. Do not shortlist, write domain state,
contact anyone, browse an authenticated source, or imply that a candidate is
eligible without cited evidence.

Complete only when the output schema validates and every material factual
claim has a source locator. Otherwise return the closed error contract.
