# secrets — never commit
.env
.env.prod
*.pem
*.key

# runtime state
sessions.db
.portal_secrets.json
artifacts/
.extracted/
.pytest_cache/
.venv/
__pycache__/
*.pyc

# ADK local caches
.adk/

# reference material / unrelated binaries — keep out of the submission repo
/*.pdf
/ssh-lambda.docx
tests/fixtures/launchvic_guidelines.pdf

# OS / editor junk
.DS_Store
~$*
